[package]
name = "uacrypt"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "CLI over dstu-core"

[lib]
name = "uacrypt"

[dependencies]
dstu-core = { path = "../dstu-core", version = "0.0.0" }
